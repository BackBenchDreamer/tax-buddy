import Settings from '@/components/Settings';

export const metadata = {
  title: 'Settings — Tax Buddy',
  description: 'Configure your Tax Buddy preferences and API keys',
};

export default function SettingsPage() {
  return <Settings />;
}

// Made with Bob
